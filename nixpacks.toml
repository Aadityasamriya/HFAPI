[phases.setup]
nixPkgs = ['python311', 'cacert']

[phases.install]
cmds = [
    'pip install --upgrade pip setuptools wheel',
    'pip install --no-cache-dir -r requirements.txt'
]

[start]
cmd = 'python main.py'

[variables]
PYTHONPATH = '.'
PYTHONUNBUFFERED = '1'
PYTHONDONTWRITEBYTECODE = '1'