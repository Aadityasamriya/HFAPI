# Core Telegram Bot Dependencies - ESSENTIAL
python-telegram-bot[ext]==22.4

# AI and ML Dependencies - ESSENTIAL
huggingface_hub==0.34.5

# Environment and Configuration - ESSENTIAL
python-dotenv>=1.0.1

# HTTP and Network - ESSENTIAL
requests==2.32.4
aiohttp>=3.10.0

# Database Dependencies - ESSENTIAL
pymongo[srv]>=4.8.0
motor>=3.5.1
asyncpg
psycopg2-binary
sqlalchemy[asyncio]

# Core Image and Document Processing - ESSENTIAL
Pillow==11.0.0
PyMuPDF>=1.24.0
python-magic>=0.4.27
filetype>=1.2.0

# Async and Performance - ESSENTIAL
asyncio-throttle>=1.0.2
tenacity>=8.5.0
typing-extensions>=4.12.0

# Security and System - ESSENTIAL
cryptography>=42.0.8
psutil>=6.0.0

# REMOVED FOR RAILWAY.COM DEPLOYMENT OPTIMIZATION
# The following heavy dependencies have been removed to ensure reliable Railway builds:
# numpy>=2.0.0              # 47MB+ - Only needed for advanced numerical operations
# opencv-python>=4.8.0      # 88MB+ - Only needed for advanced computer vision
# pdf2image>=1.16.3         # Requires poppler system dependencies
# pytesseract>=0.3.10       # Requires tesseract system dependencies
#
# These can be re-added later if specific features requiring them are needed.
# For Railway deployment, we prioritize build reliability and faster deployments.